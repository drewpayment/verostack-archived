import { CurrencyInputPipe } from './currency-input.pipe';

describe('CurrencyInputPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyInputPipe();
    expect(pipe).toBeTruthy();
  });
});
