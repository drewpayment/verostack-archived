import { PayrollDetailTotalsPipe } from './payroll-detail-totals.pipe';

describe('PayrollDetailTotalsPipe', () => {
  it('create an instance', () => {
    const pipe = new PayrollDetailTotalsPipe();
    expect(pipe).toBeTruthy();
  });
});
